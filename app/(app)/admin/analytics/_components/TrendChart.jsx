// Shared since the chart renders on both /admin (overview graph) and
// /admin/analytics (analysis card). Single implementation lives in
// components/loom/TrendChart.jsx.
export { TrendChart, ChartSkeleton } from "@/components/loom/TrendChart";
